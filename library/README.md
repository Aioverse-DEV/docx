# Library

Reference collection for source artifacts and reusable assets.

Suggested structure:

- `library/inbox/` - raw ingested documents
- `library/normalized/` - converted canonical markdown/doc outputs
- `library/snippets/` - reusable code/config/document snippets
- `library/index/` - searchable metadata indexes
